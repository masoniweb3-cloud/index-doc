# Signa Index 剩余九张信息图提示词｜母版锁定版

## 使用方法

每次只生成一张，并上传两张参考图：

1. `00-signa-index-visual-style-anchor-zh-CN.png`：唯一视觉母版；
2. 当前任务指定的旧 Doc 图片：只参考信息结构。

不要再上传 VIS、产品截图或其他生成结果，避免风格权重分散。

---

## 02｜事件与状态

结构参考：`markets-lifecycle.png`

输出文件：`02-event-vs-state-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已经批准的 Signa Index 视觉母版，是本图唯一的视觉权威。严格继承它的深海军蓝背景、暖白衬线标题、中文无衬线标签、哑光纸张、半透明深色校准片、精细连接线、圆形节点、柔和空间深度、翡翠绿与琥珀色比例。
- Image 2 是旧 Doc 的结构参考。只学习它如何表达生命周期，不复制其颜色、英文、人物或具体图标。

Primary request:
制作“事件与状态”信息图，解释事件合约具有有限生命周期，而世界状态持续存在。构图应是一件完整的、有体量的时间结构装置，不是上下两张卡片，也不是普通二维时间轴。

Composition:
- 上方或画面较高位置是一条由三张现代哑光事件票据构成的有限事件轨道：事件 A、事件 B、事件 C 依次出现，各自有明确终止边缘。
- 三张票据可以轻微错层，具有和母版一致的纸张体量与边缘处理。
- 每张事件票据的终点通过一个小型终止节点表示“到期”，不要使用日历或沙漏图标。
- 下方是一条横贯画面的翡翠绿色“长期世界状态”连续带。它经过三个事件区间但始终不中断，并在画面右侧继续延伸。
- 使用少量细垂直连接线，将每张事件票据与下方状态带对应起来，表达事件只是状态的局部观察窗口。
- 主视觉必须是“上方多个有限片段”与“下方一条连续状态带”的对比。

Text (verbatim; do not add other text):
“事件与状态”
“短期事件合约”
“事件 A”
“事件 B”
“事件 C”
“到期”
“长期世界状态”
“事件是有限命题；状态是长期坐标。”

Constraints:
- “到期”可以在三个事件终点分别出现。
- 不加入日期、倒计时、概率、价格或结算结果。
- 不画人物、日历、时钟、沙漏、K 线或普通商业时间轴。
- 不照搬母版中的三层“因果—方向—暴露”装置；只继承材质和设计语言。
- 不使用四边大框、等宽卡片、霓虹描边、玻璃拟态 UI、粗白箭头或无意义装饰。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 03｜世界状态指数的构造流程

结构参考：`overview-four-stages.png`

输出文件：`03-index-construction-flow-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已经批准的 Signa Index 视觉母版，是本图唯一的视觉权威。严格继承其背景、排版、现代哑光材质、半透明结构层、精细线路、节点、柔和阴影和克制配色。
- Image 2 是旧 Doc 的流程结构参考，只参考清晰的步骤组织，不复制视觉风格和内容。

Primary request:
制作“世界状态指数的构造流程”信息图。用一条连续、有体量的构造路径串联六个步骤，让人顺着同一条结构自然读完，而不是把六个步骤装进六张相同卡片。

Composition:
- 使用一条从左下方向右上方推进的连续装配路径，可轻微转折，但阅读顺序必须唯一。
- 六个步骤分别形成六个功能明确、形态不同但属于同一材质家族的工作节点：
  1. 一块目标定义铭牌；
  2. 一组事件合约票据；
  3. 一个方向校准结构；
  4. 一组暴露权重调节轨道；
  5. 一个多路价格汇聚节点；
  6. 一条从旧周期平稳接入新周期的连续指数带。
- 每个节点旁放置清楚的编号和步骤名称。
- 第一步至第五步以暖白、深蓝灰和少量状态色表现；第六步使用翡翠绿强调连续性。
- 不需要额外说明小字，重点是六步的准确顺序和不同功能。

Text (verbatim; do not add other text):
“世界状态指数的构造流程”
“1 定义目标状态”
“2 选择事件合约”
“3 统一事件方向”
“4 建立暴露权重”
“5 聚合价格变化”
“6 滚动接续”
“事件会到期，状态持续存在。”

Constraints:
- 编号必须完整、顺序准确，不能重复或缺失。
- 不增加第七步，不把 AI 画成独立步骤。
- 不做六张等宽卡片，不做蜂窝图，不做环形循环图，不做 SaaS onboarding 流程。
- 图形必须有填充体量，不能退化成稀疏线框图。
- 不使用机器人、齿轮堆叠、灯泡、奖杯或无意义图标。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 04｜统一事件方向

结构参考：`resolution-finalization-paths.png`

输出文件：`04-direction-mapping-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已批准的视觉母版。严格继承其纸质事件票据、半透明深色校准层、细线路、圆形节点、方向色彩、空间深度和排版。
- Image 2 只作为双路径逻辑参考，不复制其旧版视觉。

Primary request:
制作“统一事件方向”信息图，解释同样是 YES 价格上升，对目标状态可能产生相反作用，因此必须通过方向变量统一。

Composition:
- 使用上下两条尺寸平衡但不被大框包围的事件路径。
- 上方是一张“事件 A”票据，YES 价格向上，通过一块方向校准片后输出翡翠绿色路径，指向“目标状态增强”，并显示 dᵢ = +1。
- 下方是一张“事件 B”票据，YES 价格同样向上，通过同一类校准片后输出珊瑚红路径，指向“目标状态减弱”，并显示 dᵢ = −1。
- 两条路径在中部共享一个“目标状态”坐标基准，使正反方向一眼可比较。
- +1 与 −1 使用母版相同的小型深色方向标记；不要使用表情、人物或价值判断图标。

Text (verbatim; do not add other text):
“统一事件方向”
“事件 A”
“YES ↑”
“目标状态增强”
“dᵢ = +1”
“事件 B”
“目标状态减弱”
“dᵢ = −1”
“YES / NO 是合约方向，不是状态方向。”

Constraints:
- “YES ↑”可以在两条路径各出现一次。
- 数学符号必须正确：上方 +1，下方 −1。
- 绿色与红色只表示相对目标状态的方向，不表达道德或政治立场。
- 不做两个大卡片，不使用对勾和叉号，不使用天平、投票人物或政治图标。
- 不使用霓虹发光、粗箭头或普通二维流程模板。
- 所有中文和公式逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 05｜状态暴露与权重

结构参考：`markets-fee-breakdown.png`

输出文件：`05-state-exposure-weighting-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已批准的视觉母版，是唯一视觉权威。继承其深色空间、哑光实体、半透明校准层、细线路、节点、排版与克制色彩。
- Image 2 只参考“多个因素汇入一个计算结果”的信息结构，不复制旧风格。

Primary request:
制作“状态暴露与权重”信息图，解释三个评价维度如何形成原始暴露 aᵢ，并进一步归一化为权重 wᵢ。

Composition:
- 左侧布置三块具有轻微前后层次的功能校准片，分别为“因果强度”“信息独立性”“价格质量”。
- 每块校准片都有一条可见但克制的输入轨道和一个不同位置的圆形状态节点，表达三个不同评价维度；不要生成具体数值。
- 三条轨道在中央汇入一枚有体量的“原始暴露 aᵢ”汇聚件。
- 从汇聚件进入一个琥珀色边缘的归一化窄层，最终输出一条清晰的翡翠绿色“归一化权重 wᵢ”带。
- 右侧或下方放置两条精确公式，公式像母版中的 Iₜ 一样使用等宽数学排版。

Text (verbatim; do not add other text):
“状态暴露与权重”
“因果强度”
“影响是否直接、明确且重要”
“信息独立性”
“是否提供新的状态信息”
“价格质量”
“价格是否连续、可观察”
“原始暴露 aᵢ”
“归一化权重 wᵢ”
“wᵢ = aᵢ / Σaⱼ”
“Σwᵢ = 1”
“权重表示有效状态暴露，不代表新闻热度。”

Constraints:
- aᵢ、wᵢ、Σ、下标 i 和 j 必须正确清晰。
- 不生成百分比、评分数字、饼图、仪表盘、星级或进度条。
- 三个维度地位平行，不擅自排序。
- 公式区域必须清楚，不能做成极小脚注。
- 不做三张普通卡片连接第四张结果卡片。
- 所有中文和公式逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 06｜指数计算示例

结构参考：`markets-payout-calculation.png`

输出文件：`06-index-calculation-example-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档计算信息图

Input images:
- Image 1 是已批准的视觉母版。继承其深海军蓝背景、暖白纸张、等宽数字、翡翠绿与珊瑚红数据语义、柔和阴影和高精度排版。
- Image 2 只参考计算过程的清晰层级，不复制其视觉样式。

Primary request:
制作“指数计算示例”信息图。准确性优先于装饰。画面主体是一张悬浮于深色背景上的现代哑光计算底稿，由三行事件记录、贡献汇总轨道和最终指数点位组成；不要做成 Dashboard 或电子表格截图。

Composition:
- 上方标题旁放置弱化但清楚的标签“示例数据，不代表真实指数”。
- 中央暖白计算底稿包含五列和 A、B、C 三行；行列必须严格对齐。
- 每行右侧的贡献沿细线路汇入下方 rₜ 汇总。
- 正贡献使用翡翠绿小节点，负贡献使用珊瑚红小节点；文字和数字仍保持高对比可读。
- 底部按照 rₜ → I₀ → I₁ 的顺序排布三个计算步骤，最终“105.23”可略微放大，但保持编辑型克制。

Table text and data (reproduce exactly):
“事件” | “权重 wᵢ” | “方向 dᵢ” | “价格变化 Δpᵢ” | “贡献 wᵢdᵢΔpᵢ”
“A” | “50%” | “+1” | “+0.08” | “+0.040”
“B” | “30%” | “−1” | “−0.05” | “+0.015”
“C” | “20%” | “+1” | “−0.02” | “−0.004”

Other text (verbatim; do not add other text):
“指数计算示例”
“示例数据，不代表真实指数”
“rₜ = 0.040 + 0.015 − 0.004 = 0.051”
“I₀ = 100”
“I₁ = 100 × exp(0.051) = 105.23”

Constraints:
- 竖线只是表格字段分隔说明，不要在图片中显示竖线字符。
- B 行的方向和价格变化都是负数，贡献必须是 +0.015。
- C 行的价格变化与贡献必须是负数。
- 不改写任何数字、符号、小数位或公式。
- 不加入货币符号、日期、真实指数名称或收益解释。
- 不生成电脑屏幕、App UI、Excel 界面或大面积彩色表格。
- 所有文字、数字和公式逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 07｜滚动接续

结构参考：`resolution-settlement-window.png`

输出文件：`07-rolling-continuity-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已批准的视觉母版。继承其事件票据、半透明结构层、翡翠绿连续带、圆形节点、琥珀色转换标记、深色背景和空间深度。
- Image 2 只参考审查窗口与前后周期的逻辑，不复制旧版视觉。

Primary request:
制作“滚动接续”信息图，解释当前周期的事件 A、B 在审查时点退出，下一周期的事件 C、D 进入，但指数点位保持连续。

Composition:
- 用一条从左至右横贯画面的翡翠绿色连续指数带作为主视觉。它在任何位置都不能断裂或发生机械跳变。
- 中央竖立一块与母版相同材质的半透明“审查时点”转换门，边缘使用少量琥珀色。
- 左侧“当前周期”包含事件票据 A、B。两张票据沿精细轨道完成当前周期后，从转换门侧面平稳退出。
- 右侧“下一周期”包含事件票据 C、D。两张票据从转换门另一侧沿新轨道进入。
- 事件票据的进入与退出发生在指数带周围，但不能切断或推动指数带。
- 指数带穿过转换门时保持同一高度和厚度，之后继续呈现轻微连续变化。

Text (verbatim; do not add other text):
“滚动接续”
“当前周期”
“事件 A”
“事件 B”
“审查时点”
“下一周期”
“事件 C”
“事件 D”
“退出”
“进入”
“连续指数点位”
“成分不断变化，指数保持连续。”

Constraints:
- 不加入日期、频率、权重或点位数字。
- 指数带必须连续，无断点、台阶或跳空。
- 不使用循环箭头、日历、时钟、沙漏、传送带工厂或旋转木马隐喻。
- 不做左右两张大卡片，不做普通时间轴模板。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 08｜价格、映射与指数

结构参考：`overview-incentives.png`

输出文件：`08-ai-responsibility-boundary-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已批准的视觉母版。其事件票据、三层映射板、连续指数带和排版已经天然表达三种职责，必须严格继承同一视觉语言。
- Image 2 只参考职责分工的组织逻辑，不复制人物、图标或旧版风格。

Primary request:
制作“价格、映射与指数”职责边界信息图。明确预测市场形成价格，AI 映射层建立结构，指数方法完成聚合；AI 不负责价格发现。

Composition:
- 左侧“预测市场”使用一组现代事件合约票据和微型价格标记，形成可观察价格变化。
- 中央“AI 映射层”使用三块半透明校准层，分别表现因果、方向、暴露与信息重叠的结构判断。这里不出现价格数字。
- 右侧“指数方法”将经过方向调整的多条细线路汇聚成一条翡翠绿色连续指数带。
- 三个职责区通过一条明确但精细的信息路径连接；视觉上必须看出价格来自左侧市场，而不是中央 AI 层。
- 底部将“AI 不负责价格发现。”作为边界结论，排版方式继承母版底部图注。

Text (verbatim; do not add other text):
“价格、映射与指数”
“预测市场”
“形成事件价格”
“提供可观察价格变化”
“AI 映射层”
“建立事件与状态因果关系”
“识别方向、暴露与信息重叠”
“指数方法”
“聚合方向调整后的价格变化”
“生成连续指数序列”
“AI 不负责价格发现。”

Constraints:
- 不画机器人、大脑、人脸、聊天气泡、芯片、魔法光效或 AI 头像。
- AI 映射层不能生成价格、概率或指数点位。
- 三个职责区的文字不能互换。
- 不做三张等宽大卡片，不做组织架构图或 SaaS 功能介绍页。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 09｜指数不等于产品

结构参考：`earning-two-reward-streams.png`

输出文件：`09-index-vs-linked-product-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档信息图

Input images:
- Image 1 是已批准的视觉母版。特别继承其翡翠绿指数带、中央基准节点、琥珀色引用路径和中性产品凭证。
- Image 2 只参考上游基准与下游产品的区分逻辑，不复制奖励、人物、钱币或旧版配色。

Primary request:
制作“指数不等于产品”信息图，清楚区分规则化指数基准与引用该基准的产品层。指数位于上游，产品只引用指数，并拥有独立条款和风险。

Composition:
- 左侧或画面中央偏左建立一条有体量的翡翠绿色“指数层”主脊柱。
- 指数层周围依次组织四个结构要素：目标状态、成分与方向、权重与点位、规则化计算。它们共同形成指数，而不是四张产品卡片。
- 指数主脊柱在一个清晰的圆形基准节点结束。
- 从基准节点引出多条琥珀色“引用”路径，连接右侧三个中性、现代的产品凭证。
- 产品凭证区域标注四项独立要求：期限与结算、费用与融资、流动性与退出、产品特有风险。
- 指数层和产品层之间保持清晰空间边界，但不使用一条粗大分割墙。

Text (verbatim; do not add other text):
“指数不等于产品”
“指数层”
“目标状态”
“成分与方向”
“权重与点位”
“规则化计算”
“引用”
“产品层”
“期限与结算”
“费用与融资”
“流动性与退出”
“产品特有风险”
“同一指数可以被不同产品引用。”

Constraints:
- 引用方向只能从指数层指向产品层。
- 不画资金流入指数，不暗示指数接受存款、发行凭证或产生收益。
- 不加入 APY、收益率、金额、代币、交易按钮或真实产品名称。
- 不把期限、费用、融资或产品风险放入指数层。
- 不使用金币、钱包、礼物、奖杯或奖励流隐喻。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```

---

## 10｜世界状态指数的四类重点风险

结构参考：`trust-risk-categories.png`

输出文件：`10-four-key-risk-categories-zh-CN.png`

```text
请调用 image generation 能力，直接生成一张完整信息图，不要先输出分析文字。

Use case: infographic-diagram
Asset type: Signa Index 正式中文文档风险信息图

Input images:
- Image 1 是已批准的视觉母版。继承其深海军蓝空间、现代哑光凭证、半透明深色层、暖白文字、圆形节点、柔和阴影和克制的状态色。
- Image 2 只参考风险类别的清晰分组，不复制盾牌、人物、旧版配色或卡片样式。

Primary request:
制作“世界状态指数的四类重点风险”信息图。把四类风险组织在一张完整的现代风险登记底稿上，而不是四张彼此独立的 Dashboard 卡片。

Composition:
- 画面中央是一张有体量的深色“风险登记底稿”，材质与母版半透明映射层和产品凭证一致。
- 底稿内部通过精细的十字分隔和不同边缘标签形成四个区域，但仍然是一件完整对象。
- 四个风险区不是完全相同的模板：通过不同的线路异常、节点偏差、数据断点和产品分支结构，分别对应四种真实风险机制。
- 事件市场风险：表现流动路径变窄、价差扩大和价格节点跳变。
- 模型与映射风险：表现方向路径错位、因果层不一致和信息线路重叠。
- 数据与结算风险：表现数据线路中断、更正节点、争议分叉和作废终点。
- 产品特有风险：表现指数基准节点之后的杠杆、路径、流动性和对手方分支。
- 珊瑚红只用于风险异常点和边缘提示，不能让整张图成为红色警报海报。

Text (verbatim; do not add other text):
“世界状态指数的四类重点风险”
“事件市场风险”
“流动性、价差与价格跳变”
“模型与映射风险”
“方向、因果与信息重叠判断”
“数据与结算风险”
“数据中断、更正、争议与作废”
“产品特有风险”
“杠杆、路径依赖、流动性与对手方”
“指数是一种结构化视角，不是完整现实。”
“本图为重点摘要；完整风险见正文。”

Constraints:
- 不生成风险概率、评分、严重度等级、仪表盘或热力图。
- 不使用盾牌、锁、警察、火焰、骷髅、感叹号三角形或灾难插画。
- 不做四张悬浮圆角卡片；四类风险必须属于一张完整的风险登记结构。
- 不暗示四类风险穷尽全部风险。
- 不增加投资建议或保证性表述。
- 所有中文逐字准确，无水印。

Output: one complete 1536 × 1024 landscape PNG.
```
